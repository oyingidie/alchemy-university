// TEST CASE
/*
const assert = require('assert');
const {a, b} = require('../1-multiple_variables');

it('a and b should be equal', () => {
    assert.equal(a, b);
});
*/

const a = 16;
const b = a;

module.exports = { a, b }
