// TEST CASE
/*
const assert = require('assert');
const {a, b} = require('../2-booleans');

it('a should be boolean', () => {
    assert.equal(typeof a, 'boolean');
});

it('b should be boolean', () => {
    assert.equal(typeof b, 'boolean');
});

it('a should not equal b', () => {
    assert.notEqual(a, b);
});
*/

const a = false;
const b = true;

module.exports = { a, b }
