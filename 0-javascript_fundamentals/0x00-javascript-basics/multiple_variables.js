const
const 

module.exports = { a, b }

// Test Case
/*  const assert = require('assert');
    const {a,b} = require('../index');

    it('a and b should be equal', () => {
        assert.equal(a,b);
    });
*/
